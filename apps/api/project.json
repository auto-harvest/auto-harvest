{
  "name": "report-server",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/api/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "report-server:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "report-server:build:development"
        },
        "production": {
          "buildTarget": "report-server:build:production"
        }
      }
    },
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "yarn nx run report-server:build --configuration production",
          "docker build -f apps/api/Dockerfile . -t report-server"
        ]
      }
    },
    "docker-tar-send": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "docker save report-server | gzip > report-server.tar.gz",
          "scp -P 22 report-server.tar.gz mememachine@localhost:/home/mememachine/Documents/Server/Containers/Backend/report-server.tar.gz",
          "ssh -p 22 mememachine@localhost 'docker load -i /home/mememachine/Documents/Server/Containers/Backend/report-server.tar.gz'",
          "rm report-server.tar.gz"
        ]
      }
    }
  }
}
