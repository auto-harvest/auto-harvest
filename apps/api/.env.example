# MQTT Configuration
# In production, set these environment variables in your deployment platform
# Use mqtt:// for unencrypted, mqtts:// for TLS
# Standard ports: 1883 (unencrypted), 8883 (TLS)
MQTT_BROKER_URL=mqtt://192.168.100.102:1883
MQTT_TOPIC=sensor-data
MQTT_USERNAME=yourUser
MQTT_PASSWORD=yourPass

# Optional: Instance ID for multi-instance deployments
# INSTANCE_ID=instance-1

# Server Configuration
PORT=3333
NODE_ENV=production

# MongoDB Configuration
MONGODB_CONNECTION_STRING=mongodb://myuser:mypassword@192.168.100.102:27017
MONGODB_DATABASE=auto-harvest

# JWT Configuration
JWT_SECRET=your_jwt_secret
